
const state = {
    modal: false,
}

const getters = {
}

const mutations = {
    alterModal(state){ state.modal = !state.modal },
}

const actions = {
}

export default {
    state, getters, mutations, actions, namespaced: true
}
